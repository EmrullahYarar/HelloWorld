#include <stdio.h>

int main() {
    for (int i = 0; i <= 72; i++) {         // 'H'
        if (i == 72) printf("%c", i);
    }
    for (int i = 0; i <= 101; i++) {        // 'e'
        if (i == 101) printf("%c", i);
    }
    for (int i = 0; i <= 108; i++) {        // 'l'
        if (i == 108) printf("%c", i);
    }
    for (int i = 0; i <= 108; i++) {        // 'l'
        if (i == 108) printf("%c", i);
    }
    for (int i = 0; i <= 111; i++) {        // 'o'
        if (i == 111) printf("%c", i);
    }
    for (int i = 0; i <= 32; i++) {         // ' '
        if (i == 32) printf("%c", i);
    }
    for (int i = 0; i <= 87; i++) {         // 'W'
        if (i == 87) printf("%c", i);
    }
    for (int i = 0; i <= 111; i++) {        // 'o'
        if (i == 111) printf("%c", i);
    }
    for (int i = 0; i <= 114; i++) {        // 'r'
        if (i == 114) printf("%c", i);
    }
    for (int i = 0; i <= 108; i++) {        // 'l'
        if (i == 108) printf("%c", i);
    }
    for (int i = 0; i <= 100; i++) {        // 'd'
        if (i == 100) printf("%c", i);
    }
    for (int i = 0; i <= 33; i++) {         // '!'
        if (i == 33) printf("%c\n", i);
    }

    return 0;
}
